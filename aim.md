## Aim

To simulate a stereo camera system and study how depth information is recovered from the horizontal pixel shift (disparity) between two camera views, by varying the baseline distance, focal length, and object distance, and observing the resulting depth map and depth uncertainty output.
