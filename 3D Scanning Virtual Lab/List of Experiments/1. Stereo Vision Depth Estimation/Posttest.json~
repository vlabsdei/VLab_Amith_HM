{
  "version": 2.0,
  "questions": [
    {
      "question": "Q1. During the simulation, you set B = 10 cm, f = 700 px, and Z = 2 m. The disparity was approximately 35 px. You then moved the object to Z = 4 m without changing any other setting. What disparity value did the simulation display?",
      "answers": {
        "a": "70 px",
        "b": "35 px",
        "c": "17.5 px",
        "d": "140 px"
      },
      "explanations": {
        "a": "Incorrect. 70 px is the disparity at Z = 1 m for these settings. Since depth and disparity are inversely related (Z = fB/d), moving the object farther halves the disparity — it does not double it. Doubling distance halves disparity.",
        "b": "Incorrect. 35 px was the disparity at Z = 2 m. Moving the object farther always reduces disparity. If disparity stayed at 35 px, the computed depth would remain 2 m, not change to 4 m. The simulation would not show the object at 4 m with an unchanged disparity.",
        "c": "Correct! Using d = (f × B) / Z: d = (700 × 0.10) / 4 = 70 / 4 = 17.5 px. When depth doubles from 2 m to 4 m, disparity halves from 35 px to 17.5 px. This is the inverse relationship you directly observed in the simulation when dragging the object depth slider.",
        "d": "Incorrect. 140 px corresponds to an object at Z = 0.5 m — four times closer than the original 2 m. Moving the object to 4 m (twice the distance) reduces disparity by half, not increases it. This answer confuses the direction of the inverse relationship."
      },
      "correctAnswer": "c",
      "difficulty": "medium"
    },
    
    
    {
      "question": "Q2. In the simulation, you compared two runs at Z = 3 m, f = 700 px, σ = 2 px with different baselines — Run A used B = 5 cm and Run B used B = 15 cm. Which observation correctly describes what you saw on the uncertainty map?",
      "answers": {
        "a": "Run A showed lower uncertainty because a smaller baseline reduces occlusion at the edges",
        "b": "Both runs showed identical uncertainty because the object distance Z did not change",
        "c": "Run B showed lower uncertainty because a larger baseline produces larger disparities, reducing relative error",
        "d": "Run A showed lower uncertainty because more of the scene is visible with a narrower baseline"
      },
      "explanations": {
        "a": "Incorrect. Occlusion is a separate phenomenon affecting the blind zone at image edges, not depth accuracy for visible objects. Depth uncertainty is governed by ΔZ = Z²Δd/(fB), where B is in the denominator. A smaller B means larger ΔZ — Run A has worse accuracy, not better.",
        "b": "Incorrect. Object distance Z was the same in both runs, but baseline B was different. Since B appears in the denominator of the error formula ΔZ = Z²Δd/(fB), a different B produces a different ΔZ even when Z is constant. The uncertainty maps are visibly different in the simulation.",
        "c": "Correct! With B = 15 cm, the denominator (f × B) = 700 × 0.15 = 105, versus 700 × 0.05 = 35 for Run A. The uncertainty formula ΔZ = Z²Δd/(fB) gives Run B three times lower error than Run A. Run B's uncertainty map showed a noticeably cooler, less intense colour over the object, confirming better accuracy. Wider baseline → larger disparity → smaller relative error.",
        "d": "Incorrect. A narrower baseline does provide a wider shared scene overlap in theory, but scene visibility and depth accuracy are separate concerns. Depth accuracy on a per-pixel basis is determined by the uncertainty formula, not by how much of the scene is visible. The uncertainty map specifically measures measurement reliability, not scene coverage."
      },
      "correctAnswer": "c",
      "difficulty": "medium"
    },
    
    
    {
      "question": "Q3. You set σ = 4 px noise on disparity. The depth map at Z = 1 m looked acceptable, but at Z = 5 m the same noise level made the depth map practically unusable. Which formula correctly explains why the same disparity noise causes much larger problems at greater distances?",
      "answers": {
        "a": "Z = (f × B) / d — depth is inversely proportional to disparity",
        "b": "ΔZ = Z² × Δd / (f × B) — depth uncertainty grows with the square of distance",
        "c": "X = (u − cx) × Z / fx — back-projection scales XY coordinates with depth",
        "d": "Z_min = (f × B) / d_max — minimum measurable depth defines the near range limit"
      },
      "explanations": {
        "a": "Incorrect. This formula correctly computes the nominal depth value from disparity. However, it does not explain noise propagation. You need the error propagation formula to understand why the same disparity noise σ causes larger damage to the depth estimate at greater distances. This formula shows what depth is, not how uncertainty grows.",
        "b": "Correct! ΔZ = Z²Δd/(fB) directly shows that depth uncertainty scales with Z squared. At Z = 1 m: ΔZ = (1 × 4)/(700 × 0.10) ≈ 0.057 m. At Z = 5 m: ΔZ = (25 × 4)/(700 × 0.10) ≈ 1.43 m — 25 times larger for the exact same 4 px noise. This matches what you saw: the far depth map became unreliable while the near one remained clean.",
        "c": "Incorrect. This is the back-projection formula that converts 2D pixel coordinates and depth into 3D XY positions. It explains how the point cloud is constructed from depth values. It does not model how disparity noise propagates into depth uncertainty — that is a separate error analysis.",
        "d": "Incorrect. This formula defines the closest object a stereo system can measure (hardware near-range constraint). It has no relevance to noise propagation at far distances. You observed a far-range degradation problem, and the Z_min formula addresses the opposite — the near-range limit."
      },
      "correctAnswer": "b",
      "difficulty": "medium"
    },
    
    
    {
      "question": "Q4. You set Z = 9 m and observed large black patches over the object area in the depth map. What do these black patches indicate, and which slider adjustment would most directly recover valid depth readings at this distance?",
      "answers": {
        "a": "Black means the object surface is absorbing all projected light; increase scene brightness to fix it",
        "b": "Black means disparity dropped below the minimum detectable threshold; increase baseline B to recover disparity at this range",
        "c": "Black means the object moved outside the camera field of view; reduce focal length to widen the view",
        "d": "Black means depth values exceeded the display colour scale; increase the maximum display range to reveal them"
      },
      "explanations": {
        "a": "Incorrect. Black pixels in a depth map indicate unmeasurable depth, not photometric absorption. The stereo simulation does not model light absorption. Scene brightness is a concept from the structured light experiment. In stereo vision, the depth map is based on pixel matching geometry, not light intensity.",
        "b": "Correct! At Z = 9 m with B = 10 cm and f = 700 px, the disparity is d = fB/Z = 70/9 ≈ 7.8 px. With added noise, this falls below the reliable threshold and gets marked invalid (black). Increasing baseline B increases the disparity for the same Z: at B = 20 cm, d ≈ 15.6 px — back above the reliable threshold. The simulation confirms this: widening the baseline slider reduces or eliminates the black patches at 9 m.",
        "c": "Incorrect. An object at 9 m does not exit the field of view. Field of view is an angular property set by focal length and sensor size. The object at 9 m is still in front of both cameras. Reducing focal length widens the field of view but also reduces disparity sensitivity, making the depth measurement even worse at long range.",
        "d": "Incorrect. A display range issue would show the object in an unintended colour, not as black. Black is specifically the invalid pixel marker in depth maps — it means the disparity computation failed, not that the value exists but is out of display range. The simulation colour scale has no role in causing valid readings to appear black."
      },
      "correctAnswer": "b",
      "difficulty": "medium"
    },
    
    
    {
      "question": "Q5. You ran the simulation twice with identical settings except focal length — Run 1 used f = 400 px and Run 2 used f = 800 px, both at Z = 2 m and B = 10 cm. Which pair of observations correctly describes both runs?",
      "answers": {
        "a": "Run 2 produced smaller disparity and higher depth error than Run 1",
        "b": "Run 2 produced larger disparity and lower depth error than Run 1, but with a narrower field of view",
        "c": "Run 1 produced larger disparity and lower depth error than Run 2, but with a wider field of view",
        "d": "Both runs produced identical disparity because baseline and object distance were unchanged"
      },
      "explanations": {
        "a": "Incorrect. A higher focal length produces larger disparity, not smaller. For Run 2: d = (800 × 0.10)/2 = 40 px. For Run 1: d = (400 × 0.10)/2 = 20 px. Run 2 has double the disparity and therefore lower depth error, which is the opposite of what this option states.",
        "b": "Correct! Run 2 (f = 800 px) gives d = 40 px versus Run 1's d = 20 px. More disparity means the depth formula operates with a stronger geometric signal, reducing proportional error. However, higher focal length also means a more zoomed-in field of view — you saw the scene appear more magnified in Run 2. This is the key trade-off: better depth precision at the cost of seeing less of the scene per frame.",
        "c": "Incorrect. Run 1 has lower focal length (f = 400 px) and therefore produces smaller disparity (20 px vs 40 px) and higher depth error — not lower. Run 1 does have a wider field of view, which is correct, but the statement about lower depth error for Run 1 is factually wrong.",
        "d": "Incorrect. Focal length f appears directly in the disparity formula d = fB/Z. Changing f from 400 to 800 px doubles the disparity even when B and Z are unchanged. The simulation clearly shows different disparity values and different depth map appearances for the two runs."
      },
      "correctAnswer": "b",
      "difficulty": "medium"
    },
    
    
    {
      "question": "Q6. You set f = 700 px, B = 8 cm, σ = 2 px, Z = 6 m and observed RMSE = 34.7 cm — unacceptably high. Without changing Z, which single adjustment or combination reduces depth error most effectively, and by approximately what factor?",
      "answers": {
        "a": "Reduce σ from 2 px to 1 px — error reduces by factor of 2",
        "b": "Increase B from 8 cm to 24 cm — error reduces by factor of 3",
        "c": "Increase f from 700 px to 1400 px — error reduces by factor of 2",
        "d": "Apply both option b and option c together — error reduces by factor of 6"
      },
      "explanations": {
        "a": "Partially correct, but not the most effective single adjustment. The uncertainty formula ΔZ = Z²Δd/(fB) shows that halving noise Δd halves error — a factor of 2 improvement. However, physical disparity noise cannot be reduced below a hardware minimum. This option gives factor of 2, which is less powerful than combining options b and c.",
        "b": "Partially correct, but not alone the most effective. Tripling B from 8 cm to 24 cm triples the denominator: new ΔZ = (36 × 2)/(700 × 0.24) = 72/168 ≈ 0.429 m per noise pixel — factor of 3 improvement over the original 700 × 0.08 = 56 denominator. Better than option a, but still not the maximum reduction.",
        "c": "Partially correct, but not alone the most effective. Doubling f from 700 to 1400 px doubles the denominator: new ΔZ = (36 × 2)/(1400 × 0.08) = 72/112 ≈ 0.643 m per noise pixel — factor of 2 improvement. Same gain as reducing noise, but does not achieve factor of 6.",
        "d": "Correct! Applying both B × 3 and f × 2 multiplies the denominator by 6: new ΔZ = (36 × 2)/(1400 × 0.24) = 72/336 ≈ 0.214 m per noise pixel. Error reduces by factor of 6, bringing 34.7 cm down to approximately 5.8 cm. Since B and f are both in the denominator of the uncertainty formula, their improvements multiply together rather than adding. The simulation confirms: adjusting both sliders simultaneously produces the sharpest uncertainty map reduction."
      },
      "correctAnswer": "d",
      "difficulty": "hard"
    },
    
    
    {
      "question": "Q7. In the simulation, you noticed a persistent black strip along the left edge of the depth map regardless of object placement or scene content. You tried varying focal length, noise, and object distance — the strip remained but changed width. Which explanation is correct, and which parameter most directly controls the strip width?",
      "answers": {
        "a": "The black strip is caused by dead pixels on the simulated sensor; it is a fixed hardware defect and no parameter can change its width",
        "b": "The black strip is the stereo blind zone caused by occlusion — the left camera sees a region the right camera cannot; its width grows as baseline B increases",
        "c": "The black strip is a colour scale rendering artefact; adjusting the depth display minimum value removes it completely",
        "d": "The black strip appears because the focal length is set too high, cropping the image at the edges; reducing f removes the strip"
      },
      "explanations": {
        "a": "Incorrect. Dead pixels produce fixed isolated dots or lines at the same pixel positions every time, unaffected by any optical or geometric parameter. The simulation shows a strip whose width changes with baseline — this is geometric behaviour, not a sensor defect. The strip grows and shrinks as you drag the baseline slider.",
        "b": "Correct! The left camera can see a region near the left edge of the scene that the right camera cannot — because the right camera is displaced to the right, it physically cannot look far enough left to see that region. In this zone, stereo matching fails because one image is missing. Wider baseline = larger angular separation between cameras = wider region where one camera cannot see what the other sees = wider black strip. When you drag baseline toward zero, the strip vanishes. When you increase baseline, the strip grows. This is the geometric trade-off of stereo vision.",
        "c": "Incorrect. A colour scale minimum issue would make all pixels below a depth threshold appear black uniformly across the image, affecting regions based on their depth values. The strip would shift in and out of the scene as you change object distance. The actual strip stays fixed at the image edge regardless of depth values, confirming it is a matching failure zone, not a display issue.",
        "d": "Incorrect. Focal length changes the field of view by zooming in or out. Both cameras zoom equally, so the stereo geometry and overlap region scale proportionally. A very high focal length narrows the field of view but does not create an occlusion zone at one specific edge. The edge strip grows with baseline, not with focal length."
      },
      "correctAnswer": "b",
      "difficulty": "hard"
    },
    
    
    {
      "question": "Q8. You ran three simulation trials at Z = 3 m with these settings: Trial 1 — f = 700 px, B = 10 cm, σ = 1 px. Trial 2 — f = 350 px, B = 20 cm, σ = 1 px. Trial 3 — f = 700 px, B = 10 cm, σ = 3 px. Without calculating exact values, which ordering of RMSE values is correct?",
      "answers": {
        "a": "RMSE_1 = RMSE_2 = RMSE_3 — all settings cancel out to the same result",
        "b": "RMSE_1 < RMSE_2 and RMSE_1 < RMSE_3 — Trial 1 has the best accuracy",
        "c": "RMSE_1 = RMSE_2 and both are less than RMSE_3 — Trials 1 and 2 tie, both better than Trial 3",
        "d": "RMSE_2 < RMSE_1 < RMSE_3 — Trial 2 has the best accuracy because it uses the widest baseline"
      },
      "explanations": {
        "a": "Incorrect. Trial 3 has σ = 3 px versus σ = 1 px in Trials 1 and 2. Even though the f × B products are identical, the noise term Δd in ΔZ = Z²Δd/(fB) is three times larger in Trial 3. The three RMSE values cannot all be equal.",
        "b": "Incorrect. Trial 1 and Trial 2 have different individual f and B values but identical products: f × B = 700 × 0.10 = 70 and f × B = 350 × 0.20 = 70. Since the uncertainty formula depends on f × B as a product, not individually, RMSE_1 = RMSE_2 exactly. Trial 1 is not better than Trial 2.",
        "c": "Correct! The depth uncertainty formula ΔZ = Z²Δd/(fB) depends on the product f × B, not on f and B separately. Trial 1: 700 × 0.10 = 70. Trial 2: 350 × 0.20 = 70. Identical products → identical uncertainty → RMSE_1 = RMSE_2. Trial 3 has the same f × B = 70 but three times the noise, giving three times the uncertainty. The simulation confirms: swapping f and B inversely produces the same quality depth map. This is a key insight — you can achieve equivalent depth accuracy with a narrow-lens wide-baseline camera or a telephoto narrow-baseline camera.",
        "d": "Incorrect. Trial 2's wider baseline is exactly compensated by its halved focal length. The product f × B remains 70 for both Trial 1 and Trial 2. There is no advantage for Trial 2 — the depth formula treats them as equivalent. RMSE_2 = RMSE_1, not RMSE_2 < RMSE_1."
      },
      "correctAnswer": "c",
      "difficulty": "hard"
    },
    
    
    {
      "question": "Q9. With σ = 0 px and d_min = 1 px, you found the maximum measurable depth was approximately 70 m at B = 10 cm, f = 700 px. You then doubled baseline to B = 20 cm with everything else unchanged. How did Z_max change, and what design insight does this reveal?",
      "answers": {
        "a": "Z_max decreased by half because a wider baseline creates more occlusion and reduces effective range",
        "b": "Z_max stayed the same because d_min = 1 px is a fixed property and does not change with baseline",
        "c": "Z_max doubled to approximately 140 m because Z_max = fB/d_min and B was doubled while d_min stayed at 1 px",
        "d": "Z_max quadrupled to approximately 280 m because depth range scales with B squared"
      },
      "explanations": {
        "a": "Incorrect. Occlusion affects the blind zone at the near edge of the depth map — it does not reduce maximum measurable range. Maximum range is limited by minimum detectable disparity, which is independent of occlusion. Wider baseline actually extends maximum range, not reduces it.",
        "b": "Incorrect. While d_min = 1 px remains constant, Z_max = (f × B) / d_min contains B in the numerator. When B doubles, the numerator doubles, and Z_max doubles — even though d_min is unchanged. The d_min being constant does not freeze Z_max; it means Z_max scales linearly with whatever else changes in the numerator.",
        "c": "Correct! Z_max = (f × B) / d_min = (700 × 0.20) / 1 = 140 m versus the original (700 × 0.10) / 1 = 70 m. The simulation confirmed: after doubling baseline, valid depth readings appeared at distances where only black existed before. This reveals a critical design insight for long-range stereo systems — if you need to scan objects at 100 m instead of 50 m, you need to double your baseline. Autonomous vehicle stereo cameras use baselines of 30–60 cm specifically for this reason.",
        "d": "Incorrect. Z_max scales linearly with B, not with B squared. The formula Z_max = fB/d_min shows B in the first power (linear). The Z-squared relationship appears only in the uncertainty formula ΔZ = Z²Δd/(fB). These are two different formulas describing two different aspects of stereo performance."
      },
      "correctAnswer": "c",
      "difficulty": "hard"
    },
    
    
    {
      "question": "Q10. You ran the simulation at f = 700 px, B = 10 cm, Z = 2 m, σ = 2 px, but deliberately used f = 735 px (a 5% overestimate) for the depth calculation. Which outcome best describes the simulation result, and why does this matter for real scanning practice?",
      "answers": {
        "a": "The depth map shows random noise similar to high σ settings because calibration errors behave like disparity noise",
        "b": "The depth map shows a systematic 5% overestimation of all depth values on top of the random noise from σ = 2 px",
        "c": "The depth map shows a systematic 5% underestimation of all depth values on top of the random noise from σ = 2 px",
        "d": "The depth map shows no visible change because the 5% focal length error falls within the noise margin of σ = 2 px"
      },
      "explanations": {
        "a": "Incorrect. A calibration error is systematic — it shifts every pixel in the same direction by the same proportion. Random noise from σ causes pixel-by-pixel independent random fluctuations in different directions. These are fundamentally different error types. The depth map pattern looks completely different: systematic error gives a smooth but incorrectly scaled depth map, while noise gives a speckled irregular one.",
        "b": "Correct! Z_computed = (f_wrong × B) / d = (735 × 0.10) / d. Since f_wrong = 700 × 1.05, every computed depth is exactly 1.05 × the true depth — a uniform 5% overestimation across the entire scene, simultaneously with the random σ = 2 px scatter. In the simulation the object's bounding box dimensions appeared ~5% larger than ground truth, while the depth values fluctuated around that overestimated mean. This demonstrates why camera calibration (Experiment 2) must be performed accurately — even small calibration errors cause systematic, accumulating measurement errors that cannot be corrected by taking more samples.",
        "c": "Incorrect. Using f_wrong = 735 px (which is larger than the true 700 px) in Z = fB/d produces larger Z values — overestimation, not underestimation. To get underestimation, you would need to use a focal length smaller than the true value (e.g., f = 665 px would underestimate by 5%).",
        "d": "Incorrect. A 5% systematic error at Z = 2 m produces a 10 cm shift in every depth reading — all pixels show ~2.1 m instead of ~2.0 m. The σ = 2 px noise contributes approximately ±5.7 cm scatter at this range. The systematic error (10 cm) is clearly larger than the noise scatter and is easily visible as a consistent offset in the depth readout and bounding box dimensions."
      },
      "correctAnswer": "b",
      "difficulty": "hard"
    }
  ]
}
